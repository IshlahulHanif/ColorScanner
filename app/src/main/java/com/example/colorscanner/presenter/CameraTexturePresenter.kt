package com.example.colorscanner.presenter

import android.content.Context
import com.example.colorscanner.contract.CameraTextureContract

class CameraTexturePresenter(private val view: CameraTextureContract.View, private val interactor: CameraTextureContract.Interactor) : CameraTextureContract.Presenter {
    override fun onViewCreated() {

    }
}